# Claude Code — [affinity.]

Start with `README.md`, then `docs/AFFINITY_PRODUCT_VISION.md` and `docs/PRODUCT_DOCTRINE.md`.

**Core promise:** never present "in-network" or "on-formulary" as ground truth — every coverage claim carries source + freshness + confidence, and the user is told what to confirm. Rank plans on **true annual cost**, not premium. Neutral (no broker funnel). **Structure before LLM** — the plain-language explainer ships flag-default-off. Log milestones in `CHANGELOG.md`.
